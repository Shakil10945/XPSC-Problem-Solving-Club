#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin>>t;
    while (t--)
    {
        int n;
        cin>>n;
        string st;
        cin>>st;

        for(int i=0,j=n;j>n/2, i<n/2; i++,j--)
        {
            if(st[i] != st[j])
            {
                
            }
        }
    }
    

    
    

    return 0;
}