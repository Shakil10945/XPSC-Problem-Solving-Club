#include <bits/stdc++.h>
using namespace std;

int n;
vector<int> vec;

void input()
{
    cin >> n;
    vec.resize(n);

    for (int i = 0; i < n; i++)
        cin >> vec[i];    
}

void solve()
{
    input();

    //set<int>ans;
    long long sum =0;
    for(int i=0;i<n; i++)
    {
        for(int j=0; j<__lg(n); j++)
        {
            
        }
    }
    
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    solve();

    return 0;
}