#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string s;
    cin>>s;
    s.push_back('i');
    s.pop_back();
    s.front();
    s.back();
    s.empty();
    s.clear();
    s.substr(1,6);
    s.substr(8);

    return 0;
}

