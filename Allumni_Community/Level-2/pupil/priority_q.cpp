#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    priority_queue<int,vector<int>,greater<int>>pq;

    pq.push(6);
    pq.pop();
    pq.top();
    pq.size();
    pq.size();
    pq.empty();

    return 0;
}

